---
layout: 'layout'
title: 'Sloths Review Leaderboard'
---
<main>
  <h1>Sloths Review Leaderboard</h1>
  <h2>2021 Cycle 3</h2>

  <div class="leaderboard">
    {% for user, count in reviews %}
      <div class="section position" data-position="{{loop.index}}">{{ loop.index }}</div>
      <div class="section name" data-position="{{loop.index}}">{{ user }}</div>
      <div class="section score" data-position="{{loop.index}}">{{ count }}</div>
    {% endfor %}
  </div>

  <aside class="footer">
    <div>Made with ♥ by <a href="https://twitter.com/chron9">Paul</a></div>
    <div>Last updated {{ site.buildTime.toLocaleString() }} </div>
  </aside>
</main>
